## se define el parametro pi
from math import pi
## se define la variable del radio
r = float(input("input el radio del circulo"))
## se imprime el resultado del area generando la operacion del radio para un circulo
print("el area del circculo con radio de  " + str(r) + " es = " + str(pi * r **2))